async function joinBet(roomId) {
  console.log("Joining bet for room:", roomId);
  // Dummy: simulate token approve & bet call
  return new Promise(resolve => setTimeout(resolve, 1000));
}

async function payout(roomId) {
  console.log("Payout STT for room:", roomId);
}
